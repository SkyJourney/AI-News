# AInews · 本机部署配置（复制为 .env，不进 git，路径因机器而异）
#
# F2.7 Phase 8 Publish 用：指向本机 Docker 编排目录下的发布脚本。
# 脚本接收一个参数（已 build 好的 dist/ 目录），负责 rsync 到数据目录 +
# 确保 nginx 常驻服务在跑 + 冒烟测试，退出码 0/非 0 决定 publish 成功与否。
# 见 /Volumes/Docker/compose/ainews/deploy.sh
AINEWS_DEPLOY_SCRIPT=/Volumes/Docker/compose/ainews/deploy.sh
